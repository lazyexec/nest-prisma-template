// ============================================================
// core/enums.prisma
// All auth-related enums — define once, use across core
// ============================================================

enum AuthProvider {
  EMAIL
  GOOGLE
  APPLE
}

enum Role {
  ADMIN
  USER
}

enum UserStatus {
  ACTIVE
  BANNED
  RESTRICTED
  SUSPENDED
}

enum TwoFactorMethodType {
  TOTP
  EMAIL_OTP
  SMS_OTP
}
